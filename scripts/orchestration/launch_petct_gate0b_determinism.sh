#!/usr/bin/env bash
# HISTORICAL_ONLY: the R12 Gate 0-B receipt remains provenance, not an active gate.
set -euo pipefail
echo "HISTORICAL_ONLY: R12 Gate 0-B cannot launch after the R13/M0-v6 cutover." >&2
exit 64

